To do.txt

Still not working, hopefully when we build the release it will work. For now let's work on a new editor or fix an old one:

Paint editor: exists, not finished.
Script/Text editor: exists, not really finished but should wait before modification.
Sound Player: exists, could use a little but more GUI wize, it only contains this the code below.
Animation editor: does not exist (stubbed), very complex custom type (wxa), like fla but not like fla at all at the same time.
Video editor: does not exist (stubbed),
Custom editor from 3rd parties at runtime level: does not exist (not stubbed), For custom extentions like vscode has extentions.